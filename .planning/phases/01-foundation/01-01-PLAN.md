---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - slideshows/mtam-homepage-demo.html
autonomous: false
requirements:
  - STRC-01
  - STRC-02
  - STRC-03
  - STRC-04
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04

must_haves:
  truths:
    - "Opening the HTML file by double-click in a browser shows a styled page with no console errors"
    - "All 6 CDN assets load successfully (Google Fonts stylesheet, 2 Phosphor Icon stylesheets, Tailwind script, GSAP core, GSAP ScrollTrigger)"
    - "Brand tokens are defined once in :root and @theme and visibly applied — changing one token hex value updates every element that uses it"
    - "DM Serif Display renders as the display font and Manrope renders as the body font (not browser defaults)"
    - "The page renders at 1440x900 with no horizontal scrollbar and content centered within a 1200px max-width container"
    - "JTBD Job Map section placeholder comments exist in Define > Locate > Prepare > Confirm > Execute order"
    - "GSAP ScrollTrigger is registered and ready for Phase 2 animations"
  artifacts:
    - path: "slideshows/mtam-homepage-demo.html"
      provides: "Complete Phase 1 scaffold — CDN stack, design tokens, layout baseline, token verification block"
      min_lines: 150
      contains:
        - "DM+Serif+Display"
        - "Manrope"
        - "@phosphor-icons/web@2.1.2"
        - "@tailwindcss/browser@4"
        - "gsap@3.14.2"
        - "ScrollTrigger"
        - "--color-brand-accent"
        - "JOB MAP: DEFINE"
        - "JOB MAP: EXECUTE"
  key_links:
    - from: "slideshows/mtam-homepage-demo.html <style type='text/tailwindcss'>"
      to: "Tailwind CDN script"
      via: "@theme block parsed by Tailwind browser compiler"
      pattern: "@theme.*--color-brand"
    - from: "slideshows/mtam-homepage-demo.html <script> block"
      to: "GSAP CDN scripts"
      via: "gsap.registerPlugin(ScrollTrigger) on DOMContentLoaded"
      pattern: "gsap\\.registerPlugin\\(ScrollTrigger\\)"
    - from: "slideshows/mtam-homepage-demo.html :root block"
      to: "Token verification block elements"
      via: "var(--color-*) references in inline styles and Tailwind classes"
      pattern: "var\\(--color-"
---

<objective>
Create the complete Phase 1 HTML scaffold file at `slideshows/mtam-homepage-demo.html` with all CDN dependencies loaded, brand design tokens defined in both `:root` and Tailwind `@theme`, JTBD Job Map section placeholders in order, GSAP ScrollTrigger initialized, and a token verification block that visually proves the design system works.

Purpose: This file is the single deliverable for the entire project. Phase 1 locks the design system foundation so Phase 2 can add section content without touching infrastructure. Every design decision (colors, fonts, spacing, layout) is defined once here and propagated everywhere via tokens.

Output: One browser-openable HTML file with no console errors, all CDN assets loaded, and a visible token demo block.
</objective>

<execution_context>
@C:/Users/hair/OneDrive/Desktop/Mock Site Business Meeting/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hair/OneDrive/Desktop/Mock Site Business Meeting/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTML scaffold with CDN stack, design tokens, and body skeleton</name>
  <files>slideshows/mtam-homepage-demo.html</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>
Create the `slideshows/` directory (it does not exist yet), then create `slideshows/mtam-homepage-demo.html` with the following exact structure:

**Document declaration and html tag:**
```html
<!doctype html>
<html lang="en" style="scroll-behavior: smooth;">
```

**Head section — in this EXACT order (CDN load order is mandatory, do not reorder):**

1. Meta tags:
   - `<meta charset="UTF-8">`
   - `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
   - `<title>MoreThanAMethod — Learn Hair Extension Techniques That Build Your Career</title>`
   - `<meta name="description" content="Education for hair stylists who are done guessing. JTBD-designed curriculum for technique mastery and client confidence.">`

2. Google Fonts (preconnect first, then stylesheet):
   - `<link rel="preconnect" href="https://fonts.googleapis.com">`
   - `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`
   - `<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Manrope:wght@400;600&display=swap" rel="stylesheet">`

3. Phosphor Icons (regular weight, then fill weight):
   - `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2/src/regular/style.css">`
   - `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2/src/fill/style.css">`

4. `:root` CSS custom properties block inside a plain `<style>` tag — define ALL tokens from UI-SPEC:
   ```css
   :root {
     /* Surfaces */
     --color-bg:           #0f0f0f;
     --color-surface:      #1a1a1a;
     --color-border:       #2a2a2a;
     /* Text */
     --color-text-primary: #f5f5f5;
     --color-text-muted:   #9ca3af;
     /* Accent */
     --color-accent:       #C9A84C;
     --color-accent-hover: #b8953e;
     /* Semantic */
     --color-error:        #ef4444;
     /* Typography */
     --font-display: 'DM Serif Display', serif;
     --font-body:    'Manrope', sans-serif;
     /* Elevation */
     --radius-card:  16px;
     --shadow-card:  0 4px 32px rgba(0, 0, 0, 0.4);
     --shadow-deep:  0 8px 48px rgba(0, 0, 0, 0.6);
   }
   ```

5. Tailwind v4 Play CDN script:
   - `<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>`

6. Tailwind `@theme` extension block inside `<style type="text/tailwindcss">` — must come AFTER the Tailwind script tag:
   ```html
   <style type="text/tailwindcss">
     @theme {
       --color-brand-bg:           #0f0f0f;
       --color-brand-surface:      #1a1a1a;
       --color-brand-border:       #2a2a2a;
       --color-brand-text:         #f5f5f5;
       --color-brand-muted:        #9ca3af;
       --color-brand-accent:       #C9A84C;
       --color-brand-accent-hover: #b8953e;
       --font-display: 'DM Serif Display', serif;
       --font-body:    'Manrope', sans-serif;
       --radius-card:  16px;
     }

     /* Font utility classes — not auto-generated from --font-* tokens in Tailwind v4 */
     @utility font-display {
       font-family: var(--font-display);
     }
     @utility font-body {
       font-family: var(--font-body);
     }
   </style>
   ```

7. GSAP scripts (core first, then ScrollTrigger):
   - `<script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js"></script>`
   - `<script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/ScrollTrigger.min.js"></script>`

**Body tag:**
- `<body class="bg-brand-bg font-body text-brand-text" style="overflow-x: hidden;">`

**Body content — in this order:**

1. A placeholder comment at the top: `<!-- Phase 1: Token Verification Block (remove in Phase 2) -->`
2. An empty `<div>` that will be filled in Task 2 with id `token-verification`
3. JTBD Job Map section placeholder comments in EXACT order (per STRC-01):
   ```html
   <!-- ================================================== -->
   <!-- JOB MAP: DEFINE — Hero, Struggle Stack, Anti-gatekeeping manifesto -->
   <!-- ================================================== -->

   <!-- ================================================== -->
   <!-- JOB MAP: LOCATE — Course topics, Educator grid, Founder identity -->
   <!-- ================================================== -->

   <!-- ================================================== -->
   <!-- JOB MAP: PREPARE — (reserved, not used in v1) -->
   <!-- ================================================== -->

   <!-- ================================================== -->
   <!-- JOB MAP: CONFIRM — Testimonials, Pricing, Value comparison -->
   <!-- ================================================== -->

   <!-- ================================================== -->
   <!-- JOB MAP: EXECUTE — Final CTA, Footer -->
   <!-- ================================================== -->
   ```
4. GSAP initialization script at end of body:
   ```html
   <script>
     document.addEventListener('DOMContentLoaded', () => {
       gsap.registerPlugin(ScrollTrigger);
       console.log('GSAP ScrollTrigger registered — ready for Phase 2 animations');
     });
   </script>
   ```

**Critical constraints:**
- Do NOT use `@import` inside any `<style>` block
- Do NOT use any hex values directly in class attributes (e.g., `bg-[#C9A84C]`) — always use token names like `bg-brand-accent`
- Do NOT add `@import "tailwindcss"` inside the `<style type="text/tailwindcss">` block — the browser bundle handles this automatically
- The `:root` block must come BEFORE the Tailwind script tag (plain `<style>`, not `type="text/tailwindcss"`)
- The `@theme` block must come AFTER the Tailwind script tag (inside `<style type="text/tailwindcss">`)
  </action>
  <verify>
    <automated>grep -c "DM+Serif+Display" slideshows/mtam-homepage-demo.html && grep -c "@phosphor-icons/web@2.1.2" slideshows/mtam-homepage-demo.html && grep -c "@tailwindcss/browser@4" slideshows/mtam-homepage-demo.html && grep -c "gsap@3.14.2" slideshows/mtam-homepage-demo.html && grep -c "JOB MAP: DEFINE" slideshows/mtam-homepage-demo.html && grep -c "JOB MAP: EXECUTE" slideshows/mtam-homepage-demo.html && grep -c "registerPlugin(ScrollTrigger)" slideshows/mtam-homepage-demo.html && grep -c "\-\-color-brand-accent" slideshows/mtam-homepage-demo.html && grep -c "\-\-color-bg:" slideshows/mtam-homepage-demo.html && grep -c "@utility font-display" slideshows/mtam-homepage-demo.html && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `slideshows/mtam-homepage-demo.html`
    - File contains `<title>MoreThanAMethod` in the head
    - File contains `<meta name="description" content="Education for hair stylists`
    - File contains `fonts.googleapis.com/css2?family=DM+Serif+Display`
    - File contains `@phosphor-icons/web@2.1.2/src/regular/style.css`
    - File contains `@phosphor-icons/web@2.1.2/src/fill/style.css`
    - File contains `@tailwindcss/browser@4`
    - File contains `gsap@3.14.2/dist/gsap.min.js`
    - File contains `gsap@3.14.2/dist/ScrollTrigger.min.js`
    - File contains `:root {` with `--color-bg: #0f0f0f` and `--color-accent: #C9A84C` and `--shadow-card` and `--shadow-deep`
    - File contains `<style type="text/tailwindcss">` with `@theme {` block containing `--color-brand-accent: #C9A84C`
    - File contains `@utility font-display` and `@utility font-body`
    - File contains `gsap.registerPlugin(ScrollTrigger)` inside a DOMContentLoaded listener
    - File contains 5 JOB MAP comments in order: DEFINE, LOCATE, PREPARE, CONFIRM, EXECUTE
    - File contains `overflow-x: hidden` on the body element
    - File contains `scroll-behavior: smooth` on the html element
    - CDN load order in head is: Google Fonts links, then Phosphor Icons links, then Tailwind script, then `<style type="text/tailwindcss">`, then GSAP scripts
    - No `@import` inside any `<style>` block
    - No hardcoded hex values in `class=""` attributes (no `bg-[#...]` patterns)
  </acceptance_criteria>
  <done>The HTML file exists at slideshows/mtam-homepage-demo.html with all 6 CDN dependencies loaded in correct order, both :root and @theme token systems defined with identical hex values, @utility rules for font-display and font-body, JTBD Job Map placeholder comments in Define>Locate>Prepare>Confirm>Execute order, GSAP ScrollTrigger registered, and desktop layout baseline applied (overflow-x hidden, scroll-behavior smooth).</done>
</task>

<task type="auto">
  <name>Task 2: Build token verification block proving the design system works</name>
  <files>slideshows/mtam-homepage-demo.html</files>
  <read_first>
    slideshows/mtam-homepage-demo.html
    .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>
Replace the empty `<div id="token-verification">` placeholder (created in Task 1) with a complete token verification section. This section visually demonstrates that every design token works. It will be removed in Phase 2.

Build the verification block as a `<section id="token-verification">` with the content container class `max-w-[1200px] mx-auto px-8` and vertical padding of `py-16` (64px = the 3xl spacing token). Use `bg-brand-bg` for background.

The verification block must contain these subsections in order:

**A. Header:**
- `<h1>` with text "Token Verification" styled with `font-display` class and `text-[56px]` (Display role from UI-SPEC), `leading-[1.1]`, `text-brand-text`
- `<p>` below it with text "Phase 1 scaffold — this block is removed in Phase 2" styled with `font-body` class, `text-brand-muted`, `text-[14px]` (Label role)

**B. Color Swatches row:**
- A `<div>` with `flex gap-6 mt-12` containing one swatch card for each of these 7 colors:
  - `--color-bg` (#0f0f0f) — label "bg"
  - `--color-surface` (#1a1a1a) — label "surface"
  - `--color-border` (#2a2a2a) — label "border"
  - `--color-text-primary` (#f5f5f5) — label "text-primary"
  - `--color-text-muted` (#9ca3af) — label "text-muted"
  - `--color-accent` (#C9A84C) — label "accent"
  - `--color-accent-hover` (#b8953e) — label "accent-hover"
- Each swatch card: a `<div>` with `flex flex-col items-center gap-2`
  - A 64x64px color box: `<div style="width: 64px; height: 64px; border-radius: var(--radius-card); background-color: var(--color-{name}); border: 1px solid var(--color-border);"></div>`
  - A label: `<span class="text-brand-muted text-[14px]">{label}</span>`
  - A hex value: `<span class="text-brand-muted text-[14px]">{hex}</span>`

**C. Typography Samples row:**
- A `<div>` with `mt-12 space-y-6`
- Display sample: `<h2 class="font-display text-[36px] leading-[1.2] text-brand-text">DM Serif Display — Heading (36px/400)</h2>`
- Subheading sample: `<h3 class="font-body text-[16px] font-semibold leading-[1.3] text-brand-text">Manrope Semibold — Subheading (16px/600)</h3>`
- Body sample: `<p class="font-body text-[16px] leading-[1.6] text-brand-text">Manrope Regular — Body copy for paragraph text. Education for hair stylists who are done guessing. (16px/400)</p>`
- Label sample: `<p class="font-body text-[14px] leading-[1.4] text-brand-muted">Manrope Regular — Label / Caption / Metadata (14px/400)</p>`

**D. Spacing Scale row:**
- A `<div>` with `mt-12`
- Heading: `<h2 class="font-display text-[36px] leading-[1.2] text-brand-text mb-6">Spacing Scale</h2>`
- A `<div>` with `flex items-end gap-4` containing 8 vertical bars, one per spacing token:
  - xs (4px): bar height 4px, width 48px, `bg-brand-accent`
  - sm (8px): bar height 8px
  - md (16px): bar height 16px
  - lg (24px): bar height 24px
  - xl (32px): bar height 32px
  - 2xl (48px): bar height 48px
  - 3xl (64px): bar height 64px
  - 4xl (96px): bar height 96px
- Each bar: `<div class="flex flex-col items-center gap-1"><div class="bg-brand-accent" style="width: 48px; height: {value}px; border-radius: 4px;"></div><span class="text-brand-muted text-[14px]">{name}</span></div>`

**E. Component Samples row:**
- A `<div>` with `mt-12 space-y-6`
- Heading: `<h2 class="font-display text-[36px] leading-[1.2] text-brand-text mb-6">Component Tokens</h2>`

- **Card sample:** A `<div>` with `bg-brand-surface rounded-[16px] p-6` and `style="box-shadow: var(--shadow-card);"` containing:
  - `<h3 class="font-body text-[16px] font-semibold text-brand-text">Card Component</h3>`
  - `<p class="font-body text-[14px] text-brand-muted mt-2">Uses --color-surface, --radius-card, --shadow-card tokens</p>`

- **Button sample:** A `<button>` with `bg-brand-accent text-brand-bg font-body font-semibold text-[16px] px-8 py-3 rounded-[16px] cursor-pointer transition-colors duration-200 hover:bg-brand-accent-hover` with text "Start Learning"

- **Icon sample:** A `<div>` with `flex items-center gap-4 mt-4`:
  - `<i class="ph ph-arrow-right text-brand-accent text-[24px]"></i>` with label "ph-arrow-right"
  - `<i class="ph-fill ph-star text-brand-accent text-[24px]"></i>` with label "ph-fill ph-star"
  - `<i class="ph ph-check-circle text-brand-accent text-[24px]"></i>` with label "ph-check-circle"

**F. Accent Usage Reference:**
- A `<div>` with `mt-12 border-t border-brand-border pt-8`
- A thin horizontal rule in accent color: `<div class="h-[2px] bg-brand-accent w-32 mb-4"></div>`
- Text: `<p class="text-brand-muted text-[14px]">Accent (#C9A84C) reserved for: CTA buttons, hover border glow, section divider lines, active nav links, decorative pull-quotes, star/rating icons</p>`

**Critical constraints:**
- Use Tailwind utility classes (`bg-brand-accent`, `text-brand-muted`, etc.) for all styling where possible — this proves the @theme token system generates utilities correctly
- Use `var(--color-*)` inline styles only for the color swatch background-color (to demonstrate :root tokens) and for box-shadow
- Every text element must use either `font-display` or `font-body` class — no browser defaults
- Do NOT use any weight other than 400 (default) or 600 (`font-semibold`) — the type system has exactly 2 weights
- Do NOT use any font size other than 14px, 16px, 36px, or 56px — the type system has exactly 4 sizes
  </action>
  <verify>
    <automated>grep -c "token-verification" slideshows/mtam-homepage-demo.html && grep -c "Token Verification" slideshows/mtam-homepage-demo.html && grep -c "DM Serif Display" slideshows/mtam-homepage-demo.html && grep -c "Manrope" slideshows/mtam-homepage-demo.html && grep -c "Start Learning" slideshows/mtam-homepage-demo.html && grep -c "ph ph-arrow-right" slideshows/mtam-homepage-demo.html && grep -c "ph-fill ph-star" slideshows/mtam-homepage-demo.html && grep -c "bg-brand-accent" slideshows/mtam-homepage-demo.html && grep -c "shadow-card" slideshows/mtam-homepage-demo.html && grep -c "Spacing Scale" slideshows/mtam-homepage-demo.html && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - File contains `<section id="token-verification">`
    - File contains `<h1` with text "Token Verification" using `font-display` class and `text-[56px]`
    - File contains 7 color swatch divs using `var(--color-bg)`, `var(--color-surface)`, `var(--color-border)`, `var(--color-text-primary)`, `var(--color-text-muted)`, `var(--color-accent)`, `var(--color-accent-hover)`
    - File contains typography samples with all 4 declared sizes: `text-[14px]`, `text-[16px]`, `text-[36px]`, `text-[56px]`
    - File contains "DM Serif Display" and "Manrope" as visible text labels in the typography section
    - File contains spacing scale bars for all 8 tokens (4px through 96px)
    - File contains a card div with `bg-brand-surface` and `style="box-shadow: var(--shadow-card);"`
    - File contains a `<button>` with text "Start Learning" and classes `bg-brand-accent` and `hover:bg-brand-accent-hover`
    - File contains Phosphor icon elements: `ph ph-arrow-right`, `ph-fill ph-star`, `ph ph-check-circle`
    - File contains accent usage reference text mentioning "CTA buttons"
    - No font sizes other than 14px, 16px, 36px, 56px appear in text-[] classes
    - No font weights other than default (400) and font-semibold (600) are used
    - All text elements use either `font-display` or `font-body` class
  </acceptance_criteria>
  <done>The token verification block is visible in the HTML file, demonstrating all 7 brand colors as swatches, all 4 typography roles with correct fonts/sizes/weights, the 8-step spacing scale, a sample card with shadow, a CTA button with hover state, and Phosphor icons — all rendered via the token system (no hardcoded hex in classes).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify HTML scaffold in browser</name>
  <files>slideshows/mtam-homepage-demo.html</files>
  <action>
Human verification checkpoint. Open the completed HTML scaffold in a browser and verify all design tokens, CDN assets, and layout baseline work correctly. This task requires no code changes — it is a visual inspection gate before Phase 1 is marked complete.

Steps for the human verifier:
1. Double-click `slideshows/mtam-homepage-demo.html` to open in default browser
2. Check dark background, serif heading font, sans-serif body font
3. Check 7 color swatches, gold CTA button with hover, 3 Phosphor icons
4. Check 8 spacing scale bars, card with shadow
5. Open DevTools Console — confirm no red errors (yellow Tailwind CDN warning is expected)
6. Resize browser — confirm no horizontal scrollbar
  </action>
  <what-built>
Complete Phase 1 HTML scaffold at slideshows/mtam-homepage-demo.html with:
- Full CDN stack (Google Fonts, Phosphor Icons, Tailwind v4, GSAP + ScrollTrigger)
- Brand design tokens in :root and Tailwind @theme
- Token verification block showing colors, typography, spacing, card, button, icons
- JTBD Job Map section placeholders in order
- Desktop layout baseline (1200px max-width, centered, no horizontal scroll)
  </what-built>
  <how-to-verify>
1. Double-click `slideshows/mtam-homepage-demo.html` to open in your default browser
2. Check that the page has a dark (#0f0f0f near-black) background — NOT white
3. Check that "Token Verification" heading renders in DM Serif Display (a serif font, NOT sans-serif)
4. Check that body text renders in Manrope (a geometric sans-serif)
5. Check that 7 color swatches are visible, with the gold accent (#C9A84C) clearly distinguishable
6. Check that the "Start Learning" button has a gold background and changes shade on hover
7. Check that 3 Phosphor icons (arrow, star, checkmark) are visible next to the button section
8. Check that the spacing scale shows 8 bars of increasing height
9. Open browser DevTools (F12) > Console tab — confirm NO red error messages (a Tailwind CDN yellow warning about "not for production" is expected and OK)
10. Resize browser window — confirm no horizontal scrollbar appears and content stays centered
  </how-to-verify>
  <verify>Human opens file in browser, confirms all visual checks pass, no console errors</verify>
  <done>Human has verified the scaffold renders correctly: dark background, correct fonts, color swatches visible, button hover works, icons render, no console errors, no horizontal scroll.</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. `slideshows/mtam-homepage-demo.html` exists and opens by double-click
2. No red console errors in browser DevTools (yellow Tailwind CDN warning is expected)
3. All 6 CDN assets load (fonts render, icons render, Tailwind classes apply, GSAP logs to console)
4. Token verification block visually demonstrates colors, fonts, spacing, card shadow, button hover, icons
5. JTBD Job Map comments exist in correct order (grep for "JOB MAP:" returns DEFINE, LOCATE, PREPARE, CONFIRM, EXECUTE)
6. Desktop layout: no horizontal scrollbar at 1440px width, content within 1200px centered container
</verification>

<success_criteria>
- Single self-contained HTML file at slideshows/mtam-homepage-demo.html (STRC-02, STRC-03)
- CDN-only external dependencies — no npm, no build step, no server (STRC-02)
- JTBD Job Map section order established as comments: Define > Locate > Prepare > Confirm > Execute (STRC-01)
- Desktop-first layout with 1200px max-width, 32px gutters, no horizontal overflow (STRC-04)
- Polished token verification block with modern typography proving design system works (DSGN-01)
- Brand tokens use MTAM colors (near-black + warm gold) and fonts (DM Serif Display + Manrope) (DSGN-02)
- Smooth scroll behavior applied, GSAP ScrollTrigger initialized for Phase 2 (DSGN-03)
- Black/white base with #C9A84C warm gold accent visible in button and icon samples (DSGN-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
