---
phase: 02-page-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [slideshows/mtam-homepage-demo.html]
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, HERO-05, CONT-01, CONT-02, CONT-03]

must_haves:
  truths:
    - "Hero headline describes stylist outcome, not platform features — no feature-count or 'Access/Get + product noun' patterns"
    - "Struggle section names 5 specific stylist frustrations in second-person before any solution content appears"
    - "Manifesto block names identity frustrations first, then states MTAM philosophy — no product pitch"
    - "Topics section uses outcome language for every card heading — no feature counts"
    - "Educator grid shows 8 real MTAM educators with correct names and specializations"
    - "Founders section tells struggle-origin narrative — struggle first, inflection, founding decision"
    - "No brand name, product noun, or price mention appears in DEFINE zone (hero, struggle, manifesto)"
    - "Token verification block is removed before any section content appears"
  artifacts:
    - path: "slideshows/mtam-homepage-demo.html"
      provides: "DEFINE + LOCATE zone HTML sections"
      contains: "id=\"hero\""
    - path: "slideshows/mtam-homepage-demo.html"
      provides: "Struggle Stack section"
      contains: "id=\"struggle\""
    - path: "slideshows/mtam-homepage-demo.html"
      provides: "Anti-gatekeeping manifesto"
      contains: "id=\"manifesto\""
    - path: "slideshows/mtam-homepage-demo.html"
      provides: "Course topics grid"
      contains: "id=\"topics\""
    - path: "slideshows/mtam-homepage-demo.html"
      provides: "Educator grid"
      contains: "id=\"educators\""
    - path: "slideshows/mtam-homepage-demo.html"
      provides: "Founders section"
      contains: "id=\"founders\""
  key_links:
    - from: "slideshows/mtam-homepage-demo.html"
      to: "Phase 1 @theme tokens"
      via: "Tailwind utility classes"
      pattern: "bg-brand-(bg|surface|accent)|text-brand-(text|muted|accent)|border-brand-(border|accent)|font-display|font-body"
    - from: "slideshows/mtam-homepage-demo.html"
      to: "Phosphor Icons CDN"
      via: "icon font classes"
      pattern: "ph-fill ph-(check|star|user)"
---

<objective>
Build the DEFINE zone (hero, struggle stack, anti-gatekeeping manifesto) and LOCATE zone (course topics, educator grid, founders story) in the MTAM homepage HTML file.

Purpose: These 6 sections are the first half of the JTBD job-map visitor journey — the visitor defines their goal/struggle (DEFINE) and then locates the resources they need (LOCATE). All content uses outcome-based language with zero feature-count patterns, enforcing the JTBD methodology that makes this demo valuable.

Output: The HTML file gains 6 new content sections replacing the Phase 1 placeholder comments. The token verification block is removed.
</objective>

<execution_context>
@C:/Users/hair/OneDrive/Desktop/Mock Site Business Meeting/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hair/OneDrive/Desktop/Mock Site Business Meeting/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-page-sections/02-RESEARCH.md
@.planning/phases/02-page-sections/02-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@slideshows/mtam-homepage-demo.html

<interfaces>
<!-- Phase 1 established patterns the executor must follow -->

From slideshows/mtam-homepage-demo.html:
- Body classes: `class="bg-brand-bg font-body text-brand-text"`
- Container pattern: `<div class="max-w-[1200px] mx-auto px-8">`
- Section pattern: `<section id="{id}" class="py-[96px] bg-brand-bg">`
- Alternate surface: `bg-brand-surface` (for manifesto, testimonials, pricing, CTA, footer)
- CTA button: `class="bg-brand-accent text-brand-bg font-body font-semibold text-[16px] px-8 py-4 rounded-[16px] cursor-pointer transition-colors duration-200 hover:bg-brand-accent-hover"`
- Card pattern: `class="bg-brand-surface rounded-[16px] p-6"`
- Token verification block: Lines 77-201 (must be fully removed)
- JTBD zone comments: Lines 203-221 (replace with actual section HTML)
- GSAP script block: Lines 223-228 (preserve, do not modify in this plan)

Available Tailwind utilities (from Phase 1 @theme):
- Backgrounds: bg-brand-bg, bg-brand-surface, bg-brand-border, bg-brand-accent, bg-brand-accent-hover
- Text: text-brand-text, text-brand-muted, text-brand-accent
- Borders: border-brand-border, border-brand-accent
- Fonts: font-display, font-body
- Typography sizes: text-[14px], text-[16px], text-[36px], text-[56px] ONLY
- Typography weights: font-normal (400), font-semibold (600) ONLY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove verification block and build DEFINE zone (hero, struggle, manifesto)</name>
  <files>slideshows/mtam-homepage-demo.html</files>
  <read_first>
    - slideshows/mtam-homepage-demo.html (current state — must read full file to understand structure)
    - .planning/phases/02-page-sections/02-UI-SPEC.md (visual contract — exact classes, copy, layout for each section)
    - .planning/phases/02-page-sections/02-RESEARCH.md (JTBD content specs — struggle stack items, manifesto copy, anti-patterns to avoid)
  </read_first>
  <action>
    **Step 1: Remove token verification block.**
    Delete everything from `<!-- Phase 1: Token Verification Block (remove in Phase 2) -->` (line 77) through the closing `</section>` of `id="token-verification"` (line 201). Keep the blank line after it.

    **Step 2: Replace the DEFINE zone comment with three sections.**

    Replace the comment block:
    ```
    <!-- ================================================== -->
    <!-- JOB MAP: DEFINE — Hero, Struggle Stack, Anti-gatekeeping manifesto -->
    <!-- ================================================== -->
    ```

    With the following three sections IN THIS EXACT ORDER:

    **Section 1: Hero (`#hero`) — per HERO-01, HERO-02, HERO-03**

    ```html
    <!-- JOB MAP: DEFINE -->
    <section id="hero" class="py-[96px] bg-brand-bg">
      <div class="max-w-[1200px] mx-auto px-8">
        <div class="hero-content max-w-[800px]">
          <h1 class="font-display text-[56px] leading-[1.1] text-brand-text mb-6">
            Build extension skills that make clients request you by name
          </h1>
          <p class="font-body text-[16px] leading-[1.6] text-brand-muted mb-8 max-w-[560px]">
            Straightforward extension education without rigid rules, brand loyalty requirements, or judgment. Christine and Alex teach you the why — so you can make the decisions.
          </p>
          <button class="bg-brand-accent text-brand-bg font-body font-semibold text-[16px] px-8 py-4 rounded-[16px] cursor-pointer transition-colors duration-200 hover:bg-brand-accent-hover">
            Start Learning
          </button>
        </div>
      </div>
    </section>
    ```

    Hero is left-aligned (no `text-center`), max-width 800px for text block. The headline (HERO-01) uses outcome language addressing the functional job. The subheadline (HERO-02) addresses emotional job (no judgment, confidence, agency). CTA (HERO-03) says "Start Learning" — job-completion language.

    **Section 2: Struggle Stack (`#struggle`) — per HERO-04**

    ```html
    <section id="struggle" class="py-[96px] bg-brand-bg">
      <div class="max-w-[720px] mx-auto px-8">
        <h2 class="font-display text-[36px] leading-[1.2] text-brand-text mb-12 text-center">
          You're not the only one who's been here.
        </h2>
        <div class="space-y-0">
          <div class="struggle-item py-6 border-b border-brand-border">
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              You learned one method from one educator and now you can't confidently serve clients who don't fit that method.
            </p>
          </div>
          <div class="struggle-item py-6 border-b border-brand-border">
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              The education world feels like a velvet rope — expensive certifications, brand loyalty, and insider access.
            </p>
          </div>
          <div class="struggle-item py-6 border-b border-brand-border">
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              You know how to place extensions but not why certain techniques work for certain hair types.
            </p>
          </div>
          <div class="struggle-item py-6 border-b border-brand-border">
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              Multi-day in-person certifications cost thousands per method — and you'd need several to serve all your clients.
            </p>
          </div>
          <div class="struggle-item py-6 border-b border-brand-border">
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              You're a working professional who should be able to learn without pledging loyalty to a brand or a founder's specific aesthetic.
            </p>
          </div>
        </div>
      </div>
    </section>
    ```

    Five struggle items covering: situational, emotional, functional, time/resource, identity layers. All second-person ("You..."). No solution content, no brand name, no product mention. Max-width 720px, centered. Each item has `py-6 border-b border-brand-border`.

    **Section 3: Anti-Gatekeeping Manifesto (`#manifesto`) — per HERO-05**

    ```html
    <section id="manifesto" class="py-[96px] bg-brand-surface">
      <div class="max-w-[800px] mx-auto px-8">
        <div class="manifesto-content">
          <div class="h-[2px] bg-brand-accent w-16 mx-auto mb-8"></div>
          <h2 class="font-display text-[36px] leading-[1.2] text-brand-text text-center mb-8">
            The industry told you there was one right way. There isn't.
          </h2>
          <div class="space-y-4 text-center">
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              Certifications that teach one brand's tools, not universal technique. Educators who can't explain why — only how. The unspoken rule that choosing one method means abandoning all others.
            </p>
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              We built MTAM on one belief: a skilled stylist understands the principles, not just the steps. When you understand why a technique works, you can adapt it for any client, any hair type, any situation. That's not a method. That's mastery.
            </p>
          </div>
        </div>
      </div>
    </section>
    ```

    Manifesto uses `bg-brand-surface` (first alternate-surface section). Accent divider line above (`h-[2px] bg-brand-accent w-16 mx-auto mb-8`). Named identity frustrations first paragraph, MTAM philosophy second paragraph. Center-aligned. Max-width 800px.

    **CRITICAL ANTI-PATTERNS TO AVOID:**
    - No hardcoded hex values in class attributes (e.g., never `bg-[#C9A84C]`)
    - No font sizes outside 14px, 16px, 36px, 56px
    - No font weights outside 400 (default) and 600 (font-semibold)
    - No brand name or product mention in hero or struggle sections (DEFINE zone rule)
    - The manifesto is the ONLY section in DEFINE zone that can mention "MTAM" (it's the philosophy bridge to LOCATE zone)
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const h=fs.readFileSync('slideshows/mtam-homepage-demo.html','utf8'); const checks=[['no token-verification',!h.includes('id=\"token-verification\"')],['hero exists',h.includes('id=\"hero\"')],['struggle exists',h.includes('id=\"struggle\"')],['manifesto exists',h.includes('id=\"manifesto\"')],['hero headline',h.includes('Build extension skills that make clients request you by name')],['CTA label',h.includes('Start Learning')],['struggle items',h.includes('struggle-item')],['manifesto accent line',h.includes('h-[2px] bg-brand-accent w-16')],['no hardcoded hex',!h.match(/class=\"[^\"]*bg-\[#[0-9A-Fa-f]/)]]; checks.forEach(([n,p])=>console.log(p?'PASS':'FAIL',n)); if(checks.some(([,p])=>!p)) process.exit(1);"
    </automated>
  </verify>
  <acceptance_criteria>
    - `id="token-verification"` does NOT appear anywhere in the HTML file
    - `id="hero"` section exists with class `py-[96px] bg-brand-bg`
    - Hero h1 contains "Build extension skills that make clients request you by name"
    - Hero p contains "Straightforward extension education without rigid rules"
    - Hero button contains "Start Learning"
    - `id="struggle"` section exists with 5 elements having class `struggle-item`
    - Struggle section heading contains "You're not the only one who's been here."
    - No struggle-item text contains "MTAM" or "MoreThanAMethod" or "our platform"
    - `id="manifesto"` section exists with class `bg-brand-surface`
    - Manifesto contains accent divider with classes `h-[2px] bg-brand-accent w-16`
    - Manifesto contains "That's not a method. That's mastery."
    - No class attribute in the file contains `bg-[#` (hardcoded hex)
    - No class attribute contains `text-[` followed by a px value other than 14px, 16px, 36px, or 56px (except icon sizing `text-[24px]` and `text-[48px]` on `ph` elements)
    - GSAP script block at end of body is preserved unchanged
  </acceptance_criteria>
  <done>DEFINE zone complete: hero with outcome headline, struggle stack with 5 named frustrations, manifesto with identity-first philosophy. Token verification block removed. No JTBD anti-patterns present (no feature counts, no solution language in DEFINE zone, no hardcoded hex).</done>
</task>

<task type="auto">
  <name>Task 2: Build LOCATE zone (topics, educators, founders)</name>
  <files>slideshows/mtam-homepage-demo.html</files>
  <read_first>
    - slideshows/mtam-homepage-demo.html (must re-read after Task 1 modifications)
    - .planning/phases/02-page-sections/02-UI-SPEC.md (exact classes and layout for topics, educators, founders sections)
    - .planning/phases/02-page-sections/02-RESEARCH.md (educator data, topic outcomes, founder narrative)
  </read_first>
  <action>
    Replace the LOCATE zone comment block:
    ```
    <!-- ================================================== -->
    <!-- JOB MAP: LOCATE — Course topics, Educator grid, Founder identity -->
    <!-- ================================================== -->
    ```

    With the following three sections IN THIS EXACT ORDER:

    **Section 4: Course Topics (`#topics`) — per CONT-01**

    ```html
    <!-- JOB MAP: LOCATE -->
    <section id="topics" class="py-[96px] bg-brand-bg">
      <div class="max-w-[1200px] mx-auto px-8">
        <h2 class="font-display text-[36px] leading-[1.2] text-brand-text mb-4 text-center">
          Learn every technique. Own every client.
        </h2>
        <p class="font-body text-[16px] leading-[1.6] text-brand-muted mb-12 text-center max-w-[560px] mx-auto">
          From beaded rows to color correction, every module is built around the technique — not a brand.
        </p>
        <div class="grid grid-cols-3 gap-8">
          {6 topic cards}
        </div>
      </div>
    </section>
    ```

    Each topic card uses this pattern:
    ```html
    <div class="bg-brand-surface rounded-[16px] p-6 topic-card">
      <i class="ph-fill ph-check text-brand-accent text-[24px] mb-4"></i>
      <h3 class="font-body font-semibold text-[16px] text-brand-text mb-2">{Outcome heading}</h3>
      <p class="font-body text-[14px] leading-[1.5] text-brand-muted">{1-sentence elaboration}</p>
    </div>
    ```

    The 6 topic cards with OUTCOME language (never feature counts):
    1. Heading: "Beaded Row Mastery" / Body: "Place beaded rows that stay secure and comfortable through your client's entire wear cycle."
    2. Heading: "K-Tip Confidence" / Body: "Build confidence applying K-tips across different hair textures and densities."
    3. Heading: "V-Light Application" / Body: "Add volume and length with V-light techniques that blend seamlessly with natural hair."
    4. Heading: "Color That Clients Screenshot" / Body: "Create the lived-in blonding and color correction results clients bring to their appointments."
    5. Heading: "Scalp Science" / Body: "Understand scalp health well enough to prevent damage and extend client appointment intervals."
    6. Heading: "Business Growth" / Body: "Price your extension services to reflect your actual skill level, not the industry average."

    **Section 5: Educator Grid (`#educators`) — per CONT-02**

    ```html
    <section id="educators" class="py-[96px] bg-brand-bg">
      <div class="max-w-[1200px] mx-auto px-8">
        <h2 class="font-display text-[36px] leading-[1.2] text-brand-text mb-12 text-center">
          Learn from the stylists behind the work
        </h2>
        <div class="grid grid-cols-4 gap-6">
          {8 educator cards}
        </div>
      </div>
    </section>
    ```

    Each educator card uses this pattern:
    ```html
    <div class="bg-brand-surface rounded-[16px] p-6 educator-card">
      <div class="bg-brand-border rounded-[16px] w-full aspect-square mb-4 flex items-center justify-center">
        <i class="ph ph-user text-brand-muted text-[48px]"></i>
      </div>
      {badge if Founder/Co-Founder}
      <h3 class="font-body font-semibold text-[16px] text-brand-text mb-1">{Name}</h3>
      <p class="font-body text-[14px] leading-[1.5] text-brand-muted">{Specialization}</p>
    </div>
    ```

    Badge (Founder/Co-Founder only):
    ```html
    <span class="inline-block bg-brand-accent text-brand-bg font-body font-semibold text-[14px] px-3 py-1 rounded-[8px] mb-2">Founder + Lead Educator</span>
    ```

    The 8 educators (VERIFIED from morethanamethod.com):
    1. Christine Woods — "Beaded rows, K-tips, color, social media" — Badge: "Founder + Lead Educator"
    2. Alex Denaro — "K-tips, business topics" — Badge: "Co-Founder + Educator"
    3. Haley Evans — "Beaded rows" — No badge (Guest Educator label not shown as badge per UI-SPEC)
    4. Yovanka Loria — "Beaded rows + curly hair" — No badge
    5. Stella Komi — "V-light application" — No badge
    6. Krista Bartik — "Lived-in blonding" — No badge
    7. Jaye Edwards — "Color correction" — No badge
    8. Anianne Rivera — "Social media for stylists" — No badge

    Only Christine and Alex get the accent badge. Guest educators show only name + specialization (no "Guest Educator" badge — per UI-SPEC, badge is for Founder/Co-Founder only).

    **Section 6: Founders (`#founders`) — per CONT-03**

    ```html
    <section id="founders" class="py-[96px] bg-brand-bg">
      <div class="max-w-[1200px] mx-auto px-8">
        <div class="grid grid-cols-2 gap-16 items-center">
          <div class="bg-brand-surface rounded-[16px] aspect-[4/5] flex items-center justify-center">
            <i class="ph ph-user-circle text-brand-muted text-[96px]"></i>
          </div>
          <div class="founders-content">
            <div class="h-[2px] bg-brand-accent w-16 mb-6"></div>
            <h2 class="font-display text-[36px] leading-[1.2] text-brand-text mb-6">
              Why Christine and Alex built this
            </h2>
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted mb-4">
              Christine spent years being told there was one right way to do extensions — one brand, one method, one set of tools. She got certified, followed the steps, and still couldn't explain to a client why she was choosing a technique over another.
            </p>
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted mb-4">
              The turning point was realizing that the "right way" was just one method's way — not the technique's truth. The principles behind great extension work aren't owned by any brand.
            </p>
            <p class="font-body text-[16px] leading-[1.6] text-brand-muted">
              Together with Alex, she built MoreThanAMethod because every stylist deserves to understand the why — not just memorize the how.
            </p>
          </div>
        </div>
      </div>
    </section>
    ```

    Founders section is 2-column: left = large photo placeholder (aspect 4:5), right = accent divider + heading + narrative. Narrative order: struggle first, inflection point, founding decision. No feature mention, no pricing, no CTA in this section.

    **CRITICAL ANTI-PATTERNS TO AVOID:**
    - No hardcoded hex values in class attributes
    - No font sizes outside 14px, 16px, 36px, 56px (exception: icon sizes `text-[24px]`, `text-[48px]`, `text-[96px]` on `ph` icon elements only)
    - Topic card headings must be outcome language — never "350+ videos" or "12 educators"
    - Educator names must match the verified list exactly
    - Founders narrative: struggle first, then inflection, then founding — never start with MTAM features
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const h=fs.readFileSync('slideshows/mtam-homepage-demo.html','utf8'); const checks=[['topics exists',h.includes('id=\"topics\"')],['educators exists',h.includes('id=\"educators\"')],['founders exists',h.includes('id=\"founders\"')],['topics heading',h.includes('Learn every technique. Own every client.')],['educators heading',h.includes('Learn from the stylists behind the work')],['founders heading',h.includes('Why Christine and Alex built this')],['6 topic cards',(h.match(/topic-card/g)||[]).length===6],['8 educator cards',(h.match(/educator-card/g)||[]).length===8],['Christine Woods',h.includes('Christine Woods')],['Alex Denaro',h.includes('Alex Denaro')],['founder badge',h.includes('Founder + Lead Educator')],['3-col topics',h.includes('grid grid-cols-3')],['4-col educators',h.includes('grid grid-cols-4')],['founders 2-col',h.includes('grid grid-cols-2')]]; checks.forEach(([n,p])=>console.log(p?'PASS':'FAIL',n)); if(checks.some(([,p])=>!p)) process.exit(1);"
    </automated>
  </verify>
  <acceptance_criteria>
    - `id="topics"` section exists with class `py-[96px] bg-brand-bg`
    - Topics heading contains "Learn every technique. Own every client."
    - Exactly 6 elements with class `topic-card` exist
    - Topics grid uses `grid grid-cols-3 gap-8`
    - Each topic card contains `ph-fill ph-check text-brand-accent` icon
    - No topic card text contains a number followed by "+" (no feature counts)
    - `id="educators"` section exists with class `py-[96px] bg-brand-bg`
    - Educators heading contains "Learn from the stylists behind the work"
    - Exactly 8 elements with class `educator-card` exist
    - Educators grid uses `grid grid-cols-4 gap-6`
    - All 8 educator names present: Christine Woods, Alex Denaro, Haley Evans, Yovanka Loria, Stella Komi, Krista Bartik, Jaye Edwards, Anianne Rivera
    - Christine Woods card contains badge text "Founder + Lead Educator"
    - Alex Denaro card contains badge text "Co-Founder + Educator"
    - No other educator cards have accent badges
    - `id="founders"` section exists with class `py-[96px] bg-brand-bg`
    - Founders heading contains "Why Christine and Alex built this"
    - Founders layout uses `grid grid-cols-2 gap-16 items-center`
    - Founders left column contains `ph ph-user-circle text-brand-muted text-[96px]`
    - Founders right column contains accent divider `h-[2px] bg-brand-accent w-16`
    - GSAP script block at end of body is preserved unchanged
  </acceptance_criteria>
  <done>LOCATE zone complete: 6 topic cards with outcome language in 3-column grid, 8 verified educator cards in 4-column grid with founder badges, founders section with struggle-origin narrative in 2-column layout. All JTBD language rules enforced (outcome-first, no feature counts).</done>
</task>

</tasks>

<verification>
After both tasks complete, verify:
1. Token verification block is fully removed (no `id="token-verification"` in file)
2. Six sections exist in correct order: hero, struggle, manifesto, topics, educators, founders
3. JTBD zone ordering preserved: DEFINE zone sections appear before LOCATE zone sections
4. All section IDs present: `#hero`, `#struggle`, `#manifesto`, `#topics`, `#educators`, `#founders`
5. No hardcoded hex values in class attributes (no `[#` pattern in class strings)
6. No font sizes outside the 4 declared sizes (14, 16, 36, 56px) except icon font sizing
7. No feature-count language in any heading (no "350+" or "12 educators" etc.)
8. No brand name / product noun in hero or struggle sections
9. GSAP script block at bottom of body preserved unchanged
10. File still opens in browser without errors (CDN stack intact, all tags properly closed)
</verification>

<success_criteria>
- The HTML file contains all 6 DEFINE + LOCATE zone sections in correct job-map order
- All 8 requirement IDs (HERO-01 through CONT-03) are addressed with specific content
- Every headline uses outcome-based language, zero feature-count patterns
- DEFINE zone (hero, struggle, manifesto) contains no solution language except the manifesto philosophy close
- All educator names match verified MTAM data
- The file remains a valid single HTML file that opens in a browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-sections/02-01-SUMMARY.md`
</output>
